@top Program { expression* }

// @skip { space | Comment }

expression {
  Identifier
}

@tokens {

  Identifier { $[a-zA-Z_0-9]+ }
  // String { '"' (!["\\] | "\\" _)* '"' }
  // Statement { 'from' }
  // Variable { $\{\{(.*?)\}\} }
  // Variable { 'var' }
  // Comment { "{#" ![\n]* }

  // space { $[ \t\n\r]+ }

}

// @detectDelim
